'use strict';

const blogPost =
const handler = function(data) {
  $('.box').on('click', );
  $('#new-blog').on('click', () => {
    events.onGetGames(data);
  });
};

module.exports = {
  handler,
};
